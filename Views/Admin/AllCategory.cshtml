@model IEnumerable<SportEquipWeb.Models.Core.Category>

@{
    ViewBag.Title = "AllCategory";
}

<h2>All Categories</h2>

<p>
    @Html.ActionLink("Add New Category", "CreateCategory")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditCategory", new { id=item.Id }) |
            @Html.ActionLink("Details", "CategoryDetail", new { id=item.Id }) |
            @Html.ActionLink("Delete", "DeleteCategory", new { id=item.Id })
        </td>
    </tr>
}

</table>
